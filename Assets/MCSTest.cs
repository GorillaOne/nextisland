using UnityEngine;
using System.Collections;
using MORPH3D;

public class MCSTest : MonoBehaviour {

	// Use this for initialization
	void Start () {
        var manager = GetComponent<M3DCharacterManager>(); 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
